import React from 'react';
import logo from '../assets/images/logo/fitmate.png';

const Header = () => (
  <div className="main-header">
    <img className="logo" src={logo} alt="logo" />
  </div>
);

export default Header;
